[Claude Haiku 4.5] Move your StartScreen component here and add a <RouterLink> to navigate to /play. The button no longer emits — it navigates. "I have a Vue 3 app with Vue Router 4. Write a HomeView.vue that imports my StartScreen component and displays it. The StartScreen's 'Play' button should navigate to the route named 'play' using useRouter().push() — but since I'm using the Options API, use this.$router.push({ name: 'play' }) instead of useRouter. Show me the full single-file component."

[Claude Haiku 4.5] `src/views/PlayView.vue`
This view contains my QuestionCard and the game state that was previously in App.vue (questions, currentIndex, score, gameState). When the game ends, use this.$router.push({ name: 'home' }) to return to the home screen.
Move startGame(), handleAnswer(), and resetGame() methods here and delete them from App.vue. Call this.startGame() in the mounted() lifecycle hook so the game begins automatically when the player navigates to /play.

[Claude Haiku 4.5] Update the style of the nav to be more coherent with the rest of the app.