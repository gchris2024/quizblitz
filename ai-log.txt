QuestionCard.vue receives the current question as a prop.
Explain what a prop is and why we pass data down from the store via the parent rather than importing useGameStore directly inside QuestionCard.
Your answer must mention: what a prop is, the direction data flows, and one reason to prefer props for presentational components.

Explain what $emit does, how the parent listens for it, and what would happen if you forgot to handle the emitted event in the parent.

Before Pinia, currentIndex, score, and gameState all lived in App.vue.
Explain what problem this causes as the component tree grows, and how moving state into a Pinia store solves it.
Your answer must mention: prop drilling, and what it means for a component to "own" state.

Explain the difference between a traditional multi-page website and a Single-Page Application. What does <router-view> do, and why does navigating between routes not reload the page?

The game uses v-if to switch between screens.
Explain the difference between v-if and v-show. Why is v-if the better choice here rather than v-show?

Implement this code into the existing architecture:
`
<div v-if="store.gameState === 'end'"> <h2>Game Over</h2> <p>You scored {{ store.score }} of {{ store.questions.length }}</p> <div v-if="!store.scoreSubmitted"> <input v-model="store.playerName" placeholder="Enter your name" /> <button @click="store.submitScore()">Submit Score</button> </div> <p v-else>Score submitted! ✓</p> <button @click="handleRestart">Play Again</button> </div>` 
Modify any existing code accordingly

Style the LeaderboardView component in the same manner as the rest of the application.

Create src/views/LoginView.vue and src/views/RegisterView.vue. Each needs a simple form with email and password fields that call store.login() or store.register() and redirect to / on success.
Add links to both in your navigation bar.
Style the new views in the same fashion as the rest of the application.

I need you to implement the following code block into the existing architecture. In the game's end screen (ScoreBoard.vue), check if the player is logged in. If they are, the Submit Score button should appear immediately (no name input needed). If they are not, show a message prompting them to log in:

`<div v-if="store.gameState === 'end'">
  <h2>Game Over</h2>
  <p>You scored {{ store.score }} / {{ store.questions.length }}</p>

  <div v-if="store.token">
    <p>Playing as {{ store.userEmail }}</p>
    <button v-if="!store.scoreSubmitted" @click="store.submitScore()">
      Submit Score
    </button>
    <p v-else>Score submitted ✓</p>
  </div>

  <div v-else>
    <p>
      <RouterLink to="/login">Log in</RouterLink> to save your score to the leaderboard.
    </p>
  </div>

  <button @click="handleRestart">Play Again</button>
</div>`
